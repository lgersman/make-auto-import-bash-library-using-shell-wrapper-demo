#!/usr/bin/env bash
envsubst > $(basename $0 .template) <<'EOL'
Demo repository exloring how to automatically provide bash functions from a separate file to make recipes   

## Prerequisites

- GNU make

- Bash

## Usage

Change to this directory and execute `make` 

--- 

EOL